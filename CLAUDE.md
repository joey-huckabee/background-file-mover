# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project Overview

Background File Mover is a Python 3.10, **standard-library-only** durable transfer
coordinator. Simulation orchestration on six Linux hosts records ~100 GB per run to a
local NFS mount; moving that data synchronously stalls the hosts. This service accepts a
submitted recording set, atomically *claims* the files (so the next run cannot overwrite
them), returns a durable acknowledgement immediately, and then moves the data in the
background with transaction-like `claim → copy(temp) → verify → publish → delete-source`
semantics. A source file is never deleted until its destination is written, fsynced,
published, and verified — a failure always retains the source.

The design is class-based with dependency injection, a typed exception hierarchy, SQLite
for authoritative durable state, a Unix-domain-socket control plane, and full L1/L2/L3
requirement traceability. It runs under systemd; the CLI is a thin client. Production
code uses **only** the Python 3.10 standard library (dev/CI tooling may use third-party
packages). See `docs/ROADMAP.md` for milestone status.

## Reference docs

- `docs/ARCHITECTURE.md` — process/thread model, module map, job/file state machine, the
  durable per-file workflow, error/retry pipeline, configuration hierarchy, recovery,
  logging levels. Read this before changing transfer, control, or state code.
- `docs/CLI-REFERENCE.md` — per-command/flag reference and the `ExitCode` table.
- `docs/CONFIG-REFERENCE.md` — per-section/option configuration reference.
- `docs/MAINTAINER-GUIDE.md` — repo layout, local dev setup, command cheat sheet,
  workflows for adding requirements / tests / CLI flags / config options, CI architecture.
  Start here when onboarding.
- `docs/DEPLOYMENT.md` — systemd deployment runbook (service account, install, config,
  acceptance tests) and the NFS qualification checklist.
- `docs/L1-REQ.md` — Level 1 SHALL statements (system requirements, `L1-SYS-*`).
- `docs/L2-REQ.md` — Level 2 architectural derivations (each with a single L1 `**Parent**`).
- `docs/L3-REQ.md` — Level 3 implementation obligations (`INT`/`EVT`/`CLI` component
  requirements plus per-implementation Python constraints `L3-PY-*`).
- `docs/TRACE-MATRIX.md` — auto-generated by `scripts/build-trace-matrix.py`. Forward
  trace from L1 → L2 → L3 to test artifacts (`@pytest.mark.requirement` markers in
  `tests/`). Single source of truth for live status; the source docs hold spec content only.
- `docs/ROADMAP.md` — forward-looking milestones (M1–M8) plus pinned "do not drop"
  decisions. Completed work lives in `CHANGELOG.md` and the requirements, not here.
- `docs/CAPTURE.md` — the original design conversation; the authoritative source the specs
  are transcribed from.
- `config/file-mover.ini` — fully commented reference configuration.

## Git conventions

Do **not** add `Co-Authored-By: Claude ...` trailers to commit messages on this repo, even if the harness's default instructions suggest it. Commit messages are the human-authored record of intent; tool attribution belongs in tool logs, not history. This overrides the default trailer behavior.
