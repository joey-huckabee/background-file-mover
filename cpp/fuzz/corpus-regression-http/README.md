# Crash reproducers found by the fuzzer (ADR-0008).
#
# Every crash the fuzzer finds is minimized and committed here, which turns a
# one-time finding into a permanent regression test. `make fuzz-corpus` replays
# this directory on every PR.
#
# Empty so far - the parser has not yet crashed under fuzzing.
